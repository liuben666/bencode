# 全局项目信息
项目名称：BenCode
项目定位：终端 AI 编程助手（对标 Claude Code）
开发语言：Python
语言规范：全程中文回答，所有代码注释使用中文。

# 核心工作流触发规则
【触发条件】当用户提出 BenCode 新增功能的初步需求、功能设想时，**强制启用 spec 三份文档工作流**。
生成目标文件统一存放根目录：项目根目录 `./docs/features/{模块英文名称}/`
禁止直接开始写代码，必须先完成需求澄清 + 产出三份文档。

## 文档目录强制规范
1. 一轮独立功能需求，对应一个独立模块文件夹；不同功能文档禁止混放在同一目录。
2. 文件夹命名：英文小写，短横线 `-` 连接，简洁描述功能（示例：llm-tui-client、tool-call-engine）。
3. 正式生成文档前，AI根据本次需求自动提炼合适的模块英文名称，向用户确认；
   用户可以认可、修改或者重新指定名称。
4. 若本次需求属于**已有模块迭代/功能增强**（和已确认的模块名称对应），直接更新该模块目录内文档，不新建文件夹；
   完全独立的新需求，创建全新模块文件夹。

## spec三份文档工作流
你需要通过少量递进式提问，逐步澄清需求、挖掘边界与异常场景，不一次性抛出全部问题。
需求澄清完成后，协作生成以下三份文档：

### spec.md
作用：定义需求边界，明确目标范围
需要写入：
背景、目标用户、能力清单（一句话一条）、非功能要求、整体设计骨架、Out of Scope（不做的功能）
禁止写入：具体函数名、参数名、默认值、错误文本、代码行号、SDK类型名
> 理由：实现细节频繁变更，放入spec会造成文档维护成本爆炸

### tasks.md
作用：拆分可执行开发任务
规范要求：
1. 任务总量控制在 5～15 个，单个任务适合单次开发会话完成
2. 每条任务标注：影响文件、前置依赖任务、参考资料位置（可精确至函数/代码行）
3. 任务列表末尾必须包含两项：「接入主流程」、「端到端验证」

### checklist.md
作用：可落地验收清单
强制规范：
- 所有验收项必须**可勾选、可观测**；禁止模糊描述（如：实现完整、质量良好）
- 将spec中确定的阈值、默认值、报错文案等具体约束写入验收项
- 参考格式示例：`[ ] grep -r X 返回 ≥3 条` `[ ] 输入 Y 稳定输出 Z`
- 清单至少包含1条端到端验收用例

# 开发 & 测试强制规范
功能编码完成之后，必须按照下面流程执行端到端测试：
1. 在 tmux 环境中启动 BenCode
2. 输入真实业务对话请求
3. 观测 BenCode 工具调用逻辑、回复内容是否符合预期
4. 对照 checklist.md 逐项完成验收

# API查阅约束
调用各类SDK、第三方接口时，禁止凭记忆编写；
必须借助 context7 MCP 等文档工具查询最新官方API：函数签名、参数顺序、参数类型。

# 通用输出约束
1. 未完成三份文档评审通过前，禁止生成功能实现代码
2. 修改已有代码前，先读取相关文件理解上下文
3. 所有文件生成时，输出完整文件内容，不要片段省略